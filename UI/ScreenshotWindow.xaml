<Window x:Class="MyQuicker.UI.ScreenshotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Black"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Grid x:Name="RootGrid" Background="Transparent" IsHitTestVisible="True">
        <!-- Base screenshot layer -->
        <Image x:Name="BackgroundImage" Stretch="None" />

        <!-- Dark mask with a hole punched out (Exclude) for the selection -->
        <Path Fill="#66000000" IsHitTestVisible="True">
            <Path.Data>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <RectangleGeometry x:Name="ScreenGeometry" />
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <RectangleGeometry x:Name="CutoutGeometry" />
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </Path.Data>
        </Path>

        <!-- Red highlight border around the selection -->
        <Border x:Name="HighlightBorder"
                BorderBrush="Red"
                BorderThickness="2"
                Visibility="Hidden" />
    </Grid>
</Window>
